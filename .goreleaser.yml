project_name: wasmdee

builds:
  - main: ./cmd/wasmdee
    binary: wasmdee
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

release:
  github:
    owner: AppajiDheeraj
    name: wasmdee
